services:
  rlcs:
    build: .
    container_name: rlcs-predictions
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    env_file:
      - .env
    volumes:
      - /var/lib/rlcs-predictions/data:/data